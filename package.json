{
  "name": "linuxdo-oauth2-wrapper",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Workers based Linux.do OAuth2 userinfo wrapper",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/linuxdo-client.js && node --check src/http.js && node --check src/userinfo-transformer.js"
  },
  "devDependencies": {
    "wrangler": "^4.20.0"
  }
}
